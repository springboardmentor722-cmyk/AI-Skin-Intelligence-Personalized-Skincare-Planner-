# M3R Task Ledger

Single live view per `milestone_3_Master_prompt.md` §7. Pre-filled at P0 with every
phase's task IDs. Status vocabulary: `TODO` · `IN_PROGRESS` · `BLOCKED` · `DONE` ·
`DEFERRED`. Governing spec for every row: `docs/milestones/milestone_3/MILESTONE 3.pdf`
(the graded rubric) + `M3R_API_CONTRACT.md` for endpoint shapes + `M3R_GAP_ANALYSIS.md`
for what's real vs missing today.

| ID | Phase | Task | Branch | Status | Notes |
|----|-------|------|--------|--------|-------|
| M3R-P0-T1 | P0 | Environment & stack sanity | feat/m3r-p0-gap-analysis | DONE | Docker stack up (postgres/mongo/redis/elasticsearch/minio/worker, all healthy). Backend suite 507/507 green incl. all 12 MinIO storage tests. Frontend `typecheck`/`lint` clean (2 pre-existing warnings, unrelated). |
| M3R-P0-T2 | P0 | Verify earlier-M3 claims | feat/m3r-p0-gap-analysis | DONE | See `M3R_GAP_ANALYSIS.md` §0-5. Outbox/worker/ES-FAISS real; ingredients router real but no safety-score endpoint; recommendations v2 real but wrong weights/categories; progress real but missing 90-day + photo metadata; analytics real but photos not merged; `ml/` harness real but standalone; frontend dashboards partial, portal missing photo/routine-overwrite entirely. |
| M3R-P0-T3 | P0 | M1/M2 pending verification | feat/m3r-p0-gap-analysis | DONE | All 6 Master Prompt §3 items re-verified; 4 of 6 turned out already stale/closed (MinIO creds, weather keys, deprecated aliases, bugs #3/#4). `docker-compose.yml` api/web absence confirmed (expected, M4 scope). ADR-010 outbox confirmed real. |
| M3R-P0-T4 | P0 | Rubric gap table | feat/m3r-p0-gap-analysis | DONE | Full requirement-by-requirement table in `M3R_GAP_ANALYSIS.md`. Real gaps: Safety Score endpoint (missing), conflict-matrix routine-step scoping (partial), recommendation weights/categories (missing/wrong), budget hard-cap (partial), 90-day adherence (missing), photo tag+score-at-upload (partial), analytics photo merge (partial), chart library (conflict, resolved), portal photo-compare + routine-overwrite (missing, blocks E2E). |
| M3R-P0-T5 | P0 | Contract freeze | feat/m3r-p0-gap-analysis | DONE | `M3R_API_CONTRACT.md` — 4 surfaces frozen: safety-score (new), recommendations (extended), progress/photos (extended), analytics (extended). |
| M3R-P0-T6 | P0 | Task ledger | feat/m3r-p0-gap-analysis | DONE | This file. |
| M3R-P1-T1 | P1 | Knowledge base audit & seed | feat/m3r-p1-ingredient-intelligence | DONE | P0 confirmed all 7 rubric classes already seeded correctly (`seed.py:230-320`) — no gap, no work needed. |
| M3R-P1-T2 | P1 | Allergy matching engine | feat/m3r-p1-ingredient-intelligence | DONE | P0 confirmed already real with alias support (`app/ai/suitability.py`) — no gap, no work needed. |
| M3R-P1-T3 | P1 | Chemical conflict matrix (routine-step scoping) | feat/m3r-p1-ingredient-intelligence | DONE | No new step-aware table needed: the safety-score endpoint's request shape (`ingredient_ids` + one `routine_time` value) already scopes to one step by construction, so every pairwise interaction found among them is inherently same-step (see `M3R_GAP_ANALYSIS.md` §1, `M3R_API_CONTRACT.md` §1). The config-driven severity thresholds (`IngredientSafetyConfig`, migration `75e0940c0f36`) are a separate T4 concern (score-formula tuning), not the step-scoping itself. |
| M3R-P1-T4 | P1 | Safety Score endpoint | feat/m3r-p1-ingredient-intelligence | DONE | `POST /api/v1/ingredients/safety-score` deployed with role/ownership checks; 10 passing tests (see T5's evidence for the breakdown). |
| M3R-P1-T5 | P1 | Tests | feat/m3r-p1-ingredient-intelligence | DONE | 10 tests across `test_ingredients_service.py`/`test_ingredients_router.py`/`test_rbac.py`: 1 config (`get_active_safety_config` seeded-row read, Task 1) + 4 `compute_safety_score` service (unsafe pairing, allergy via free-text synonym, clean list scores Safe, rejects unknown id); 4 router (unsafe pairing 200, empty list 422, professional without assignment 404, professional **with** a real assignment 200 — a genuine `verify_assignment` allow-path exercise, replacing an earlier vacuous "consultant" test that never touched the ownership check); 1 RBAC (admin role rejected 403). The "professional without assignment" 404 case lives in `test_ingredients_router.py`, not `test_rbac.py` — corrected here from an earlier misattribution. |
| M3R-P2-T1 | P2 | Catalog categories & price tiers | feat/m3r-p2-recommendation-engine | DONE | 7 rubric-literal categories mapped over real Sephora skincare catalog. Live ingested: Face Wash 219, Moisturizer 406, Serum 379, Treatment Products 356, Toner 79, Sunscreen 107, Face Masks 180, uncategorized 699 (2,425 total in live DB incl. 16 hand-seeded). Raw CSV: 8,494 total rows across all Sephora lines; 2,409 skincare-only rows ingested. |
| M3R-P2-T2 | P2 | Hard-filter safety gate | feat/m3r-p2-recommendation-engine | DONE | P0 confirmed already real and tested (`recommendations/service.py:339-352`) — no gap, no work needed. |
| M3R-P2-T3 | P2 | Suitability scoring (50/35/15) | feat/m3r-p2-recommendation-engine | DONE | Config-driven `recommendation_weights` table (migration 6d05f726e558): 50% Concern / 35% Skin-Type Fit / 15% Rating, CHECK sum = 1.00. Seeds one active row with defaults. Reads via `get_active_recommendation_weights()` service. |
| M3R-P2-T4 | P2 | Budget optimization & alternatives | feat/m3r-p2-recommendation-engine | DONE | Hard-cap + alternative substitution integrated into GET `/recommendations/me` response. Budget-flagged products (over_budget: true) paired with cheaper alternatives via alternative_for_product_id. Inline, no separate endpoint call needed. |
| M3R-P2-T5 | P2 | Recommendation endpoint (categorized, match %) | feat/m3r-p2-recommendation-engine | DONE | Per `M3R_API_CONTRACT.md` §2: results grouped by category (one top-1 per category). match_score renamed to match_percentage (0-100 scale). reasons array explains scoring per rubric factors. Service enforces one active recommendation_weights row via CHECK + unique index. |
| M3R-P2-T6 | P2 | Tests | feat/m3r-p2-recommendation-engine | DONE | Stale count corrected (was 23/2 pre-existing flaky/521): as of Task 9, **26 tests** across `test_recommendations_service.py` (21) and `test_recommendations_router.py` (5) — weight-sum read, budget-cap + alternative flagging (allergy **and** avoid-ingredient gates, Task 8's Addendum 2), per-category top-1 ranking excluding `uncategorized` (Task 9), match % 0-100, max_price filtering, feedback round-trip, ownership 403 rejection. See T7/T8/T9 rows below for what changed after this row was first written. |
| M3R-P2-T7 | P2 | Real-catalog `product_skin_types`/`product_concerns` population | feat/m3r-p2-recommendation-engine | DONE | `parse_highlights()` maps the real Sephora `highlights` column (6 `Best for...` skin-type phrases, 6 `Good for...` concern phrases — no phrase for every rubric skin type/concern exists in the real data, see the Sensitive-skin note below) onto real seeded `skin_types`/`skin_concerns` names; `load_product_associations()` idempotently upserts `product_skin_types`/`product_concerns` keyed by `(brand_name, product_name)`. Real ingest run created 2,081 skin-type links + 2,982 concern links; live DB today (after Task 9's cleanup) totals 2,129 `product_skin_types` + 3,009 `product_concerns` rows (remainder pre-existing from the 16 hand-seeded catalog). This is what turned the routine/recommendation candidate pool real (665+ products per skin type instead of ~16) — the precondition that made Task 9's allergy-gate bug reachable. 5 new tests in `test_products_ingest.py` (18 total in that file after Task 7, 19 after Task 9's ingredient-normalization regression test). |
| M3R-P2-T8 | P2 | Safety-gate fixes (2 review rounds) + routine-interaction test scope fix | feat/m3r-p2-recommendation-engine | DONE | **Round 1:** `_apply_budget_cap`'s alternative-selection query never joined `ProductSkinType` and never ran `evaluate_products_suitability` — a budget alternative could carry an allergy-flagged ingredient the main ranking path would have hard-excluded. Fixed: candidates now join `ProductSkinType`, run `evaluate_products_suitability`, drop `any_allergy` matches; alternative `match_percentage`/`active_ingredient_tags` now go through the same `ContentBasedRecommender`/`get_active_recommendation_weights` path as every other entry instead of a separate ad-hoc formula. **Round 2 (found in the SAME review, addendum):** the Round-1 rewrite, following the brief's code verbatim, silently dropped the OLD pre-Task-8 `list_avoided_ingredient_product_ids` call — `any_allergy` only covers free-text/structured allergy matches, not the separate skin-type-avoid-junction gate (e.g. Salicylic Acid avoid-flagged for Sensitive skin), so an avoid-flagged product could still slip through as a "safe" alternative. Reproduced live (read-only): a Dry-skin `max_price=98.0` request served product 3715 ("Marine Hyaluronics", Salicylic Acid) as a false-safe alternative; 23 such products were eligible in that pool. Fixed by re-adding the avoid-junction filter at the SQL layer alongside the allergy check. **Also fixed in this task:** a real regression in `test_generated_routines_never_place_two_avoid_paired_actives_together` (Task 7's real catalog legitimately combines Retinol+Salicylic-Acid in one manufacturer-formulated product; the test was flattening ingredients across products and flagging a single product's own formula as a cross-product conflict) — rescoped to compare ingredient names only across genuinely different products in a routine, not within one. Both rounds + the test-scope fix are TDD-verified (failing-then-passing), each its own commit (`219019f`, `2562d2c`, `d33dc75`). |
| M3R-P2-T9 | P2 | Second final-review fixes: allergy gate on routines/`get_alternatives`, `uncategorized` leak, ingredient-name normalization | feat/m3r-p2-recommendation-engine | DONE | A third round of the same allergy-gate class of bug T8 fixed for the budget-cap path: found (pre-existing, only reachable after T7's real candidate pool) missing from routine generation (`_generate_steps`), manual step add/update (`_assert_product_is_safe`), step-swap search (`search_products_for_edit`), and `products_service.get_alternatives` — all four now run `evaluate_products_suitability` over the same avoid-junction-narrowed pool before serving a product. TDD note: 2 of the 3 new routine tests initially passed *without* the fix (137-real-candidate Moisturizer pool made the unsafe pick too rare to reliably trigger; a `[:10]`-truncated search sorted the fresh unsafe product out by `product_id`) — both rewritten to be deterministic (RNG-forcing monkeypatch; unique-name query) before being trusted. Also fixed: `"uncategorized"` (699 real products, no clean `tertiary_category` mapping) was being served as an unintended 8th recommendation category — now hard-excluded from `served_by_category`. Also fixed: `_parse_ingredients`'s `.strip().strip("'\"")` ordering bug left stray whitespace on 17 real ingested ingredient names; changed to a single `.strip(" '\"")` pass. One-time live-DB cleanup of the pre-existing 17 polluted rows: diagnostic found **10 real name collisions with an existing canonical ingredient**, not the 1 originally suspected (Salix Alba, Peel Oil, Bark Extract, Potassium Sorbate, Carmine, a colorant named "Green 5", Salicylic Acid, Iron Oxides, Titanium Dioxide, Glycyrrhiza Glabra) — each merged (`product_ingredients` repointed to the canonical id, `ON CONFLICT DO NOTHING`, malformed row deleted) rather than renamed, since renaming would have violated the table's natural-key uniqueness; the other 7 non-colliding rows were renamed in place. Verified: re-ran the diagnostic SELECT post-cleanup, zero polluted rows remain; confirmed zero duplicate `ingredient_name` values exist; Salicylic Acid's (`ingredient_id=5`) `product_ingredients` link count grew from 197 to 199 as expected (no `ON CONFLICT` collisions on that merge). Follow-up review round found one more gap in the same allergy-gate class: the guardrail layer's soothing-product substitution (`routines/guardrails.py`, runs AFTER candidate selection) had no allergy/avoid check of its own — fixed by gating `soothing_product_id` through the same `evaluate_products_suitability` check before it's used as a substitute, falling back to leaving the original (harsh-active) step as-is when the soothing product itself is allergy-flagged, since no second soothing product exists in the catalog. |
| M3R-P3-T1 | P3 | AM/PM check-in logging (MongoDB) | feat/m3r-p3-progress-photo-pipeline | DONE | P0 confirmed already real (`routine_logs` collection via `toggle_step_completion`) — no gap, no work needed. |
| M3R-P3-T2 | P3 | Adherence math engine (7/30/90) + historical-routine lookup | feat/m3r-p3-progress-photo-pipeline | DONE | **Bug fix (commits b78c0f6):** `get_adherence_series` was judging every historical day against the currently active routine's steps, silently misjudging days before mid-window regenerations. Added `list_historical_active_step_ids` (per-day lookup against the routine active as of that day; soft-deactivated routines are never deleted, so queryable). Also added `get_compliance_percentages` (7/30/90-day completed/assigned ratios, None when nothing assigned). Migration head: `fc93ac5cf2d4` (photo metadata). 7 passing tests: `test_adherence_series_reflects_real_routine_logs`, `test_adherence_series_uses_the_routine_active_on_each_historical_day`, `test_get_compliance_percentages_computes_completed_over_assigned`, `test_get_compliance_percentages_excludes_zero_assigned_days_from_denominator`, plus 3 routine tests: `test_generates_am_pm_weekly_and_seasonal_routines`, `test_weekly_routine_is_exfoliation_only`, `test_list_historical_active_step_ids_uses_the_routine_active_on_each_day`. |
| M3R-P3-T3 | P3 | Photo pipeline metadata (score-at-upload, tag) | feat/m3r-p3-progress-photo-pipeline | DONE | **Schema + router extension (commit 5ce97b6):** new Alembic migration `fc93ac5cf2d4` adds `skin_health_score_at_upload` PG column to `progress_images` and wires `tag`/`image_stage` through `POST /progress/photos` (first photo auto-tags "Baseline", subsequent compute "Week N" from weeks-since-baseline, user-supplied tag wins over default). 2 passing tests: `test_first_photo_auto_tags_baseline_second_computes_week_number`, `test_progress_photo_score_stays_frozen_after_a_later_score_changes`. |
| M3R-P3-T4 | P3 | Analytics endpoint (merge photos in) | feat/m3r-p3-progress-photo-pipeline | DONE | **Endpoint merge (commit 1b11963):** `GET /analytics/me` now returns `photos` array (progress_image_id, image_stage, uploaded_at, skin_health_score_at_upload, url) alongside score_timeline and compliance 7/30/90 percentages. Single endpoint serves rubric's "score timelines, compliance %, and photo links" together. 1 passing test: `test_get_my_analytics_includes_compliance_and_photos`. |
| M3R-P3-T5 | P3 | Tests | feat/m3r-p3-progress-photo-pipeline | DONE | 11 passing tests across all three tasks: 7 compliance/adherence/historical (T2 tests), 2 frozen-score/baseline-tag (T3 tests), 1 analytics-merge (T4 test). All cover the real rubric surface: 90-day boundary, mid-window routine change (the bug fix's core correctness case), frozen score-at-upload, week tagging, and analytics merge. |
| M3R-P4-T1 | P4 | Score gauge + sub-scores | feat/m3r-p4-user-dashboard | DONE | `SkinScoreRingProps` gained an optional `subScores` prop (commit 63e5f0e) rendering the 5 REAL weighted components (skin_condition/lifestyle/routine_adherence/sleep_quality/hydration at 0.35/0.20/0.20/0.15/0.10) beside the gauge on the dashboard only — landing hero + assessment/results untouched, confirmed backward-compatible. Live-verified non-degenerate real values (93/43.89/100/80/66.67) for a real user. |
| M3R-P4-T2 | P4 | Central AM/PM checklist (real-time + live-sync) | feat/m3r-p4-user-dashboard | DONE | Real-time logging already existed (P0). Added `refetchInterval: 30_000` to the dashboard's routines query (commit a76e541) so a future P5 professional-overwrite shows up without a manual reload — full end-to-end proof deferred to P6, since P5's overwrite endpoint doesn't exist yet. Toggle-to-Mongo persistence live-verified (`routine_logs` gained a real `completed_steps` entry with timestamp). |
| M3R-P4-T3 | P4 | Analytics chart (Chart.js/Plotly, 7/30/90 switcher) | feat/m3r-p4-user-dashboard | DONE | Chart.js (`react-chartjs-2`) chosen over Plotly (AGENTS.md reserves Plotly for "heavy scientific viz"). New `ScoreAdherenceChart` component (commits 6026db6, df1d049) — `trend-chart.tsx` deliberately left untouched (shared by 4 other consumers with an incompatible prop shape, verified by reviewer); fed only by `GET /analytics/me`'s `score_vs_adherence`, two series (score + adherence×100), literal 7/30/90-day window switcher. One fix round: theme-color read was missing the accent-palette dependency (`usePalette()`), now fixed. Live-verified real 2-point score_vs_adherence data. |
| M3R-P4-T4 | P4 | Recommendations shelf | feat/m3r-p4-user-dashboard | DONE | `CarouselProduct` gained `matchPercentage`/`activeIngredientTags`/`overBudget` (commit 7cae3ec), wired straight from `RecommendationRead`'s real fields — literal "X% Match" text (replacing the old binary "Best Match" badge), ingredient tag chips (capped at 2, empty-safe), an "Over budget" flag badge. Live-verified real varying match% (86/85) and real budget-cap flagging (7/8 over_budget, 1 real alternative). |
| M3R-P4-T5 | P4 | States + a11y + fidelity pass | feat/m3r-p4-user-dashboard | DONE | Full details: `docs/milestones/milestone_3/build/p4-user-dashboard-fidelity.md`. Found and resolved (with owner) a real wireframe discrepancy: `app-dashboard.html`/`.png` has a mismatched "Clinical Portal"/"Dr. Sarah Chen" header + nav (matches none of the 4 locked role navs) though its body content matches the User Dashboard closely — Stitch MCP unreachable this session, so re-extraction deferred; this phase proceeded on content-only comparison per owner decision. A11y verified via static read against the dashboard's real interactive checklist, `ChecklistStrip` (a plain `<button>`, not `RoutineChecklistCard` — that component is check-in-only; corrected in a later review pass, which also added `aria-pressed` to the button). Full gate: frontend typecheck/lint/build clean; backend pytest 548 passed, 0 failed. No browser tool available in this environment (docker-compose api/web deferred to M4 by design) — pixel/visual diff and live keyboard-nav recording not possible, flagged honestly rather than fabricated; a human browser pass is recommended before treating P4 as fully shipped. |
| M3R-P5-T1 | P5 | Patient roster panel | feat/m3r-p5-professional-portal | DONE | N+1 fix confirmed still holding (LIMIT/OFFSET pattern unchanged). Added server-side search (`q` param, `ILIKE` on name/email, applied to both count and page queries) and 7/30-day compliance metrics (`compliance_seven_day`/`compliance_thirty_day` on `ClientSummaryRead`, sourced from the existing `progress_service.get_compliance_percentages` — no reimplemented math). One fix round: added the missing HTTP-level router test (new `test_clinical_review_router.py`). Live-verified: 8 real assigned clients with real varying scores; non-matching search correctly returns total=0. **Noted (final review):** computing `get_compliance_percentages` inside this loop adds the full 7/30/90-day window (extra PG queries + a Mongo round trip) per client on every roster page load, on top of the function's already-accepted N+1 shape — fine at current professional-portal scale (bounded by `page_size`), not fixed, just documented. |
| M3R-P5-T2 | P5 | Patient inspection view | feat/m3r-p5-professional-portal | DONE | New `GET /clients/{user_id}/analytics` (thin, assignment-gated, delegates to `analytics_service.get_my_analytics` — same function the user's own dashboard uses). `client-detail-view.tsx` renders the existing `ScoreAdherenceChart` (built in P4) fed by this endpoint. Review fully clean, no findings. Live-verified: 2 real score_vs_adherence points, matching the exact count found for this same user in P4's fidelity pass. **Final whole-branch review fix:** the phase spec's unconditional "'Not medical advice' disclaimer present" requirement (T2) had no corresponding text anywhere in `client-detail-view.tsx` — added inline near the score/progress-timeline card ("AI-derived insights, not a clinical diagnosis."), same visual pattern as `ingredient-detail.tsx`. |
| M3R-P5-T3 | P5 | Side-by-side photo comparison | feat/m3r-p5-professional-portal | DONE | New `GET /clients/{user_id}/photos`, delegates to `progress_service.get_progress_photos` — which already computed the before/after (oldest/newest) pair server-side, simpler than the originally planned two-dropdown selector. New `PhotoComparison` component. One fix round: a nested `StateCard` inside an already-carded wrapper produced a double-bordered empty state, fixed by swapping to the bare `WidgetEmpty` (matching the sibling Progress-timeline section's pattern). Live-verified: real honest empty state (before=None, after=None) for a client with zero photos. |
| M3R-P5-T4 | P5 | Routine-overwrite form (live sync) | feat/m3r-p5-professional-portal | DONE | 4 new assignment-gated endpoints in `clinical_review/router.py` (search/add/update/delete), each a thin wrapper delegating to the EXACT same `routines_service.add_step/update_step/delete_step/search_products_for_edit` functions the user's own routine editor already calls (single-writer rule — no duplicated mutation logic). Each successful mutation writes a real audit-log row via the existing `admin_service.write_audit_log`, with the correct commit ordering (the mutation functions already commit internally; the audit write needs its own follow-up commit — verified correct in both static review and live execution). Frontend: extracted a shared `RoutineEditor` component from the user's own ~500-line routine editor (genuine reuse, not a fork) — new professional-scoped edit screens at `/consultant/clients/[userId]/routines/[routineId]/edit` and the dermatologist equivalent. Real gap found and correctly scoped: no client-scoped step-reorder endpoint exists (the phase rubric only names "add/remove/edit", not reordering) — the professional editor hides reorder controls rather than wiring to a nonexistent endpoint, judged correctly scoped by the final task review, not a fix-now gap. Live-verified end-to-end: a real routine-step mutation via the reused service function produced exactly one real audit-log row. |
| M3R-P5-T5 | P5 | States + fidelity | feat/m3r-p5-professional-portal | DONE | Full details: `docs/milestones/milestone_3/build/p5-professional-portal-fidelity.md`. Empty roster, unassigned-client 404 (not 403 — matches `get_client_detail`'s existing convention across every endpoint in this phase, verified for all 4 new mutation endpoints too), both themes reviewed at the code/token level. Found and fixed one real a11y gap: the roster search input relied on placeholder text alone with no `aria-label` — added. Content-layout comparison against `consultant-client-detail.png`/`derm-patient-detail.png` confirmed the wireframes' invented clinical fields (Diagnostic Analysis sub-scores, AI Insights, Risk Factor Index) were already correctly excluded by an earlier phase, and P5's new sections (analytics timeline, photo comparison) follow the same real-data-only discipline. Full gate: frontend typecheck/lint/build clean (both new routes confirmed in the build output); backend pytest 557 passed, 0 failed. Same no-browser-tool environment constraint as P4 — a human browser pass is recommended before treating P5 as fully shipped. |
| M3R-P6-T1 | P6 | Rubric unit-test sweep | feat/m3r-p6-e2e-verification | DONE | Chemical-clash (`test_interactions.py`, 5 tests) and allergy-filter (`test_suitability.py` + `test_recommendations_service.py`) coverage confirmed already solid, no gap. Real gap closed in adherence formulas (`test_progress_service.py`, 3 new tests): exact-value 30/90-day compliance assertions (previously only `is not None`), a mid-window-routine-change case against `get_compliance_percentages` specifically (the existing test only covered the sibling `get_adherence_series`), and a day-boundary test using the repo's established UTC convention. Reviewer independently reproduced the mid-window test's bug-catching power by re-injecting the pre-fix bug and confirming it fails. 19/19 passing (16 pre-existing + 3 new). |
| M3R-P6-T2 | P6 | Rubric E2E walkthrough spec | feat/m3r-p6-e2e-verification | DONE | New `web/tests/e2e/m3-rubric-walkthrough.spec.ts` — assessment→recommendations→check-off→photo→dermatologist inspects+edits→user sees the revision, both themes, screenshots at every step under `docs/milestones/milestone_3/build/e2e/`. Also added a 3-viewport (375/768/1440) responsive check on the User Dashboard and Professional Portal per this session's explicit instruction. Found and fixed 2 real, previously-undetected UI bugs the new spec exposed: (1) the shared routine editor's fixed save-action bar used `w-full` on a `position:fixed` element with a `left-64` offset, extending 256px past the viewport's right edge on desktop and making Save/Discard genuinely unclickable (Playwright's click retried until timeout, which read as a hang across several diagnostic attempts before root-causing it) — fixed with `inset-x-0` + the sidebar's own real `--sidebar-width` CSS variable; (2) the topbar's weather/date widgets revealed at the exact same `md:` (768px) breakpoint the sidebar expands at, causing tablet-width horizontal overflow on both screens — fixed by bumping both to `lg:`, matching a third element in the same file already correctly gated there. 3×-consecutive flake check passed (6/6 across both themes). |
| M3R-P6-T3 | P6 | Cross-dashboard sync + persistence checks | feat/m3r-p6-e2e-verification | DONE | New `web/tests/e2e/m3-persistence-after-restart.spec.ts`. Real environment risk found and navigated: an early attempt also force-killed the real `uv run uvicorn` process to test that restart path, which tripped a Windows Application Control policy that then blocked the entire uv-managed Python toolchain system-wide, taking the backend down with no way to recover except the machine owner's own Windows Security console action (each blocked executable — `uv.exe`, `uvicorn`, `pytest` — needed its own separate exception, resolved by whitelisting the whole `.venv` folder). Scope narrowed with the owner's explicit sign-off to restart only the real `docker compose restart worker` container instead of the backend API process — documented honestly in the spec's own header; the full-backend-process-restart proof is deferred to a real deployment environment. Real DB-level persistence proven for all 3 rubric-named things (Mongo `routine_logs`, Postgres `progress_images`, Postgres `routine_products`/`routine_steps`) via direct queries after the restart, plus a fresh-HTTP cross-role check (one fix round: the dermatologist-side check initially asserted a static label that would pass regardless of persistence — fixed to assert the real swapped product name). Verified stable across 3 separate runs. |
| M3R-P6-T4 | P6 | Full-gate run | feat/m3r-p6-e2e-verification | DONE | Backend: `ruff` clean, `mypy --strict` clean (147 files), full `pytest` 560/560 passed (20m16s). Frontend: `typecheck`/`lint` clean, unit tests 14/14, production `next build` succeeds (all routes prerender). Full e2e suite (`npx playwright test`, all ~50 spec files × 2 themes): found and fixed 2 real, pre-existing bugs the full run exposed for the first time — `user-journey.spec.ts`'s ingredient search used a non-exact `getByRole` match for "Retinol" that now resolves ambiguously against the real ingested Sephora catalog (14+ ingredient names contain "Retinol" as a substring; this spec predates that real catalog) — fixed with `exact: true`; and `helpers.ts`'s shared `deleteTestUser` cleanup could hit a real FK violation if a spec's page stays open past sign-out, since a live client-side poll (the dashboard's own 30s routines refetch, P4-T2) can call the real get-or-generate routine endpoint in the background during cleanup and recreate rows the delete sequence already removed — fixed by sweeping the routine-delete a second time immediately before the FK-dependent `skin_profiles` delete. After both fixes: 99-101/101 e2e tests green per run, with the residual 2-3 that occasionally fail under the full ~101-test sequential load independently confirmed passing every time when re-run alone or in small batches — the same suite-level resource-contention pattern already established and accepted throughout this milestone (`PROGRESS.md`'s repeated `test_ingredients_service.py` ES-isolation notes), not a real regression. |
| M3R-P7-T1 | P7 | Outcomes sign-off document | chore/m3r-p7-docs-release | TODO | `M3R_COMPLETION_REPORT.md`. |
| M3R-P7-T2 | P7 | Docs in lockstep | chore/m3r-p7-docs-release | TODO | ARCHITECTURE.md, CONVENTIONS.md, ADRs for chart-library/storage/90-day decisions, `database_schemas/` mirrors, `AGENTS.md`'s stale "ingredients router lands M3" line. |
| M3R-P7-T3 | P7 | PROGRESS.md milestone entry | chore/m3r-p7-docs-release | TODO | |
| M3R-P7-T4 | P7 | Ledger close-out | chore/m3r-p7-docs-release | TODO | |
| M3R-P7-T5 | P7 | Branch cleanup | chore/m3r-p7-docs-release | TODO | Verify every `feat/m3r-*`/`fix/m3r-*`/`chore/m3r-*` branch merged + deleted. |
| M3R-P7-T6 | P7 | Handoff note for M4 | chore/m3r-p7-docs-release | TODO | `api`/`web` compose services, deprecated-alias claim correction, any deferred items. |

**Known, honest data-coverage gap (not a bug, T7/T9):** Sensitive-skin
recommendations still draw effectively only from the original 16 hand-seeded
products — the real Sephora `highlights` column has no phrase mapping to
"Sensitive" in the actual dataset (`_SKIN_TYPE_HIGHLIGHT_MAP` names 6 real
`Best for...` phrases, none of them Sensitive), so none of the 2,409 real ingested
products ever get a `product_skin_types` row for it. Confirmed live: `product_skin_types`
has 665 Normal / 660 Combination / 471 Dry / 326 Oily rows vs. **7 Sensitive** (the
hand-seeded scale). Not fabricated or guessed around (AGENTS.md §0.2) — recorded here
as a real, known limitation.

## Decisions recorded (owner confirmed this session, not silent defaults)

1. **Chart library:** switch to Chart.js or Plotly for P4/P5 dashboard charts
   (deviation from the locked Recharts default — ADR to be recorded in P7 or at P4
   kickoff).
2. **Cloud storage:** MinIO (S3-compatible) accepted as satisfying the rubric's "AWS
   S3 or Azure Blob" wording — no live cloud bucket needed.
3. **Working tree cleanup:** pre-existing uncommitted doc deletions folded into the P0
   setup commit (`bd145e2`).

## Skill/plugin discovery result

| Skill/plugin | Status |
|---|---|
| superpowers | AVAILABLE |
| ponytail | AVAILABLE (active this session, full mode) |
| graphify | AVAILABLE (`graphify-out/` exists in repo) |
| code-review | AVAILABLE |
| ui-ux-pro-max | AVAILABLE |
| frontend-design | AVAILABLE |
| find-skills | AVAILABLE |
| shadcn | to be confirmed at P4 kickoff |
| migrate-radix-to-base | to be confirmed at P4 kickoff |
